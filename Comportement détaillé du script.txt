// Jack-Josias – Créateur du projet
// Documentation mise à jour pour la v3.5 (Architecture Modulaire)

## 🔍 Comportement détaillé du script

Le script fonctionne désormais selon une architecture modulaire qui sépare clairement les responsabilités pour plus de robustesse et de clarté.

### 🤖 Flux d'Exécution

Supposons le lancement de la commande suivante :
`python codebase_extractor.py ./MonProjet -o rapport --format txt,md`

1.  **Phase 1 : Interface CLI (`codebase_extractor.py`)**
    *   Le script principal reçoit les arguments : le chemin `./MonProjet`, le nom de base `rapport`, et les formats `txt,md`.
    *   Il ne contient plus la logique métier. Son rôle est d'orchestrer les autres modules.
    *   Il instancie le moteur principal : `extractor = CodebaseExtractor(...)`.
    *   Il appelle la méthode d'extraction : `extractor.extract_codebase(...)`.

2.  **Phase 2 : Moteur d'Extraction (`src/core.py`)**
    *   La classe `CodebaseExtractor` prend le contrôle.
    *   **Normalisation :** Le chemin `./MonProjet` est converti en un chemin absolu.
    *   **Analyse `.gitignore` :** Le moteur recherche un fichier `.gitignore` à la racine de `MonProjet` et charge ses règles.
    *   **Parcours Récursif :** Il scanne tous les dossiers et fichiers de `MonProjet`, en appliquant les filtres (règles de base + `.gitignore`).
    *   **Collecte :** Il dresse la liste de tous les fichiers de code pertinents à extraire.
    *   **Extraction Parallèle :** Il lance plusieurs threads pour lire le contenu de tous les fichiers collectés simultanément, pour une vitesse maximale.
    *   **Préparation des Données :** Il assemble toutes les informations (statistiques, arborescence, contenu des fichiers) dans une structure de données unique (`report_data`).

3.  **Phase 3 : Moteur de Rendu (`src/renderers/`)**
    *   Le moteur d'extraction transmet la structure `report_data` aux modules de rendu demandés.
    *   `txt_renderer.py` prend les données et génère le contenu du fichier `rapport.txt`.
    *   `md_renderer.py` prend les mêmes données et génère le contenu du fichier `rapport.md`.
    *   Chaque renderer est spécialisé dans un seul format, ce qui rend l'ajout de nouveaux formats (ex: XML) très simple.

4.  **Phase 4 : Écriture et Post-Traitement (`codebase_extractor.py`)**
    *   Le script principal récupère le contenu généré par chaque renderer et l'écrit dans les fichiers correspondants.
    *   Si le mode interactif est activé, il peut faire appel à `src/utils.py` pour proposer la compression du rapport en une seule ligne.

### ✅ Ce que fait le script (Résumé) :

1.  **Orchestration Claire :** Le script principal dirige le flux.
2.  **Logique Centralisée :** Le module `core` gère toute la logique d'extraction.
3.  **Rendu Spécialisé :** Le module `renderers` s'occupe de la mise en forme de la sortie.
4.  **Utilitaires Isolés :** Le module `utils` fournit des fonctions annexes.
5.  **Extraction TOTALE et Parallèle :** Récupère le contenu de TOUS les fichiers pertinents à haute vitesse.
6.  **Intelligence d'Exclusion :** Ignore automatiquement les dossiers inutiles via des règles de base et le support de `.gitignore`.

### 🚀 Avantages de la nouvelle architecture :

*   **Maintenabilité :** Corriger un bug de rendu ne risque plus de casser la logique de parcours de fichiers.
*   **Testabilité :** Chaque module peut être testé de manière indépendante.
*   **Évolutivité :** Ajouter un nouveau format de rapport se résume à créer un nouveau fichier dans `src/renderers/`.
*   **Clarté :** Le code est plus facile à lire et à comprendre pour les nouveaux contributeurs.

---
**Document rédigé et maintenu par Jack-Josias (2025)**
