// Jack-Josias – Créateur du projet
// Documentation mise à jour pour la v3.5 (Architecture Modulaire)

## 📦 DÉPENDANCES DU SCRIPT

### ✅ Bonne nouvelle : TOUJOURS AUCUNE dépendance externe !

Même après sa refactorisation en une application modulaire, le script continue d'utiliser **uniquement des bibliothèques Python standard**.

```python
# Bibliothèques intégrées à Python (pas besoin d'installer)
import os
import sys
import platform
import pathlib
import mimetypes
import argparse
from datetime import datetime
import fnmatch
import json
import re
from concurrent.futures import ThreadPoolExecutor
from typing import Dict, Optional, List, Any, Tuple
```

## 🚀 PRÉREQUIS SYSTÈME

### Version de Python
Le prérequis reste le même :
```bash
# Vérifiez votre version de Python
python --version
# ou
python3 --version

# Version minimale requise : Python 3.6+
```

## 📦 INSTALLATION PYTHON (si nécessaire)

Si Python n'est pas installé sur votre système, veuillez vous référer aux guides d'installation officiels.

*   **Windows :** [python.org](https://python.org) ou Microsoft Store.
*   **macOS :** Via Homebrew (`brew install python3`) ou [python.org](https://python.org).
*   **Linux (Ubuntu/Debian) :** `sudo apt update && sudo apt install python3`

**Aucune commande `pip install ...` n'est requise pour ce script !**

## 🎯 INSTALLATION ET TEST

1.  **Télécharger le projet**
    Assurez-vous de télécharger l'ensemble du projet, y compris le répertoire `src/`.
    ```
    CodeBase-Extractor/
    ├── codebase_extractor.py
    └── src/
        └── ...
    ```

2.  **Rendre exécutable (Linux/Mac)**
    ```bash
    chmod +x codebase_extractor.py
    ```

3.  **Test rapide**
    Placez-vous dans le répertoire `CodeBase-Extractor/` et lancez :
    ```bash
    # Test de l'aide pour vérifier que les imports fonctionnent
    python3 codebase_extractor.py --help

    # Test sur un répertoire (créez un dossier de test)
    python3 codebase_extractor.py /chemin/vers/votre/projet -o test.txt
    ```

## 🎯 RÉSUMÉ ULTRA-SIMPLE

*   **Vous n'avez RIEN à installer** (à part Python 3.6+).
*   Le script fonctionne "out-of-the-box" car il n'utilise que des bibliothèques standard.
*   Assurez-vous de conserver la structure de dossiers `codebase_extractor.py` et `src/` ensemble.

C'est l'un des avantages majeurs du script : **zéro dépendance externe** pour une portabilité et une facilité d'utilisation maximales.

---
**Fichier rédigé et maintenu par Jack-Josias (2025)**